---
import Base from "../layouts/Base.astro";
---
<Base>
  <section style="min-height: calc(100vh - 60px); display: flex; align-items: center; justify-content: center;">
    <p style="font-family: var(--font-mono); color: var(--color-faint); font-size: 12px; letter-spacing: 0.1em;">
      // shell ready — sections incoming
    </p>
  </section>
</Base>
